# cheatsheet_data.py

cheatsheet = {
    "Statistics": [
        {
            "title": "Mean",
            "definition": "The average of a set of numbers.",
            "formula": "mean = sum(x) / n",
            "example": "For x = [2, 4, 6], mean = (2 + 4 + 6)/3 = 4"
        },
        {
            "title": "Standard Deviation",
            "definition": "Measures the dispersion of a dataset.",
            "formula": "σ = sqrt(sum((x - mean)^2) / n)",
            "example": "For x = [2, 4, 6], std dev ≈ 1.63"
        }
    ],
    "Machine Learning": [
        {
            "title": "Overfitting",
            "definition": "When a model learns the training data too well and fails to generalize.",
            "example": "Very low training loss but high test loss."
        }
    ]
}
